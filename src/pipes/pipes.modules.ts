import { NgModule } from '@angular/core';
import { AmountWithCurrency } from './amount-with-currency.pipe';

@NgModule({
  imports: [
  ],
  declarations: [
    AmountWithCurrency
  ],
  exports: [
    AmountWithCurrency
  ],
  providers: [
  ],
})
export class PipesModule { }
