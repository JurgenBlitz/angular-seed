/**
 * Below is an example of a model to be used in the app, currently not in use.
 * Overwrite these variables as needed.
 */
export interface UserExample {
    name: string;
    email: string;
    creationDate: Date;
    isActive: boolean;
    age: number;
}
